# -*- coding: utf-8 -*-
# vim: ft=jinja
#
# Collection of common macros

{%- macro join_if_list(value, delimiter=' ; ') -%}
  {%- if value is list -%}
    {{ value | join(delimiter) }}
  {%- else -%}
    {{ value }}
  {%- endif -%}
{%- endmacro -%}
